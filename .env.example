# Copy this file to .env.local and fill in your Supabase project's own values.
# .env.local is gitignored (matches the *.local pattern) — never commit real keys.
# Only the anon/public key belongs here. Never put the service role key in frontend env vars.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
